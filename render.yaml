services:
  - type: web
    name: canteeniq
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --worker-class=gthread --threads=4 --bind=0.0.0.0:$PORT
    plan: free
